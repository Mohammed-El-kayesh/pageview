import 'package:flutter/material.dart';

class ColorDetectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Color Detection')),

    );
  }
}
